@{
    Layout = "_PublicLayout";
    ViewBag.Title = Resource.ForPage($"{nameof(Home)}/{nameof(Home.NotFound)}");
}

<div class="title text-right">@Resource.ForPage($"{nameof(Home)}/{nameof(Home.NotFound)}")</div>

<hr />

<div class="title fa fa-search"></div>

<hr />

<div class="description">@Resource.ForString("PageNotFound")</div>

<a asp-action="@nameof(Home.Index)" asp-controller="@nameof(Home)" asp-area="" class="btn btn-block btn-primary mt-3">
    @Resource.ForAction("Home")
</a>
